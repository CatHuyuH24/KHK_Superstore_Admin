<!DOCTYPE html>
<html lang="en">

<head>
  <title><%= title %></title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <meta http-equiv="x-ua-compatible" content="ie=edge" />
  <meta name="referrer" content="always" />
  <meta name="description" content="TailwindCSS Ecommerce Template" />
  <link href="//fonts.googleapis.com/css?family=Righteous" rel="stylesheet" />
  <link href="//fonts.googleapis.com/css?family=Open+Sans+Condensed:300,300i,700" rel="stylesheet" />
  <link href="//fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i" rel="stylesheet" />
  <link rel="stylesheet" href="/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/public/css/message.css" />
  <link id="favicon" rel="icon" type="image/png" href="/images/store_icon.ico" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/tailwind.css" />
</head>

<body>
<!-- #region Header -->
<%- include('layouts/mainLayout', { content: locals.body }) %>
<!-- #region Search -->
<%- include('layouts/search')%>
<!-- #endregion Search -->
<!--region get user_id to put into cart-->
<input type="hidden" id="user-id" value="<%= user_id %>" />
<!-- #endregion Header -->

<div class="max-w-xl mx-auto my-10 bg-white shadow-md rounded-2xl p-6">
  <!-- Back Button -->
  <div class="mt-2 mb-4">
    <a href="/profile" class="flex items-center text-blue-500 hover:text-blue-700">
      <i class="fas fa-arrow-left mr-2"></i> <!-- Font Awesome arrow icon -->
      <span>Back</span>
    </a>
  </div>
  <h1 class="text-2xl font-bold text-center mb-6">Change Password</h1>
  <form id="changePasswordForm" class="space-y-4" method="POST" action="/change-password">
    <!-- Current Password -->
    <div>
      <label for="currentPassword" class="block font-medium mb-1">Current Password</label>
      <input type="password" id="currentPassword" name="currentPassword"
             class="w-full border-gray-300 rounded-lg p-2 focus:ring focus:ring-blue-300"
             placeholder="Enter your current password" required />
      <p id="currentPasswordError" class="text-red-500 text-sm mt-1 hidden">
        Current password is required.
      </p>
    </div>

    <!-- New Password -->
    <div>
      <label for="newPassword" class="block font-medium mb-1">New Password</label>
      <input type="password" id="newPassword" name="newPassword"
             class="w-full border-gray-300 rounded-lg p-2 focus:ring focus:ring-blue-300"
             placeholder="Enter your new password" required />
      <p id="newPasswordError" class="text-red-500 text-sm mt-1 hidden">
        New password is required.
      </p>
    </div>

    <!-- Confirm New Password -->
    <div>
      <label for="confirmNewPassword" class="block font-medium mb-1">Confirm New Password</label>
      <input type="password" id="confirmNewPassword" name="confirmNewPassword"
             class="w-full border-gray-300 rounded-lg p-2 focus:ring focus:ring-blue-300"
             placeholder="Confirm your new password" required />
      <p id="confirmNewPasswordError" class="text-red-500 text-sm mt-1 hidden">
        Confirm passwords do not match.
      </p>
    </div>

    <!-- ...existing code... -->
    <% if (typeof message !== 'undefined') { %>
      <% if (message === 'Password changed successfully') { %>
        <div class="text-blue-500">
          <%= message %>
        </div>
      <% } else { %>
        <div class="text-red-500">
          <%= message %>
        </div>
      <% } %>
    <% } %>
    <!-- ...existing code... -->

    <!-- Submit Button -->
    <div>
      <button type="submit" id="submitBtn"
              class="w-full bg-gradient-to-r from-blue-500 to-teal-500 text-white py-3 rounded-lg hover:from-blue-600 hover:to-teal-600 focus:outline-none focus:ring-2 focus:ring-blue-400 transition-all duration-300 transform hover:scale-105 shadow-lg">
        Change Password
      </button>
    </div>
  </form>
</div>

<!-- #region Footer-->
<%- include('layouts/footer') %>
<!-- #endregion Footer -->
<script src="/js/cart.js"></script>
<script src="https://cdn.tailwindcss.com"></script>
<script src="/js/filterHandlingAndPaginationAJAX.js"></script>
<script src="/layouts/search"></script>
<script src="/public/js/profile.js"></script>
</body>

</html>